# Fase 1 (este motor) NAO precisa de nada: roda em Python puro (stdlib).
#
# As dependencias abaixo sao OPCIONAIS, para as proximas fases (dados reais /
# graficos / execucao ao vivo). Instale apenas quando for usar, num ambiente
# com internet:
#
#   pip install -r requirements.txt
#
# ccxt          # dados e execucao em corretoras de cripto (Binance etc.)
# yfinance      # dados historicos de acoes (gratuito)
# MetaTrader5   # B3 / mini-indice via MetaTrader 5 (apenas Windows)
# matplotlib    # graficos da curva de capital
# pandas        # manipulacao de dados (opcional; o motor nao depende disso)
